name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.8.1'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Angular library
        run: npm run build ngx-obelisco-v17

      - name: Pack the library
        run: |
          cd dist/ngx-obelisco-v17
          npm pack

      - name: Set up npm authentication
        run: npm set //registry.npmjs.org/:_authToken=${{ secrets.TOKEN_NPM }}

      - name: Publish to NPM
        run: |
          cd dist/ngx-obelisco-v17
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.TOKEN_NPM }}

      - name: Run Semantic Release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_ANGULAR_TOKEN }}
          NPM_TOKEN: ${{ secrets.TOKEN_NPM }}
        run: npx semantic-release
